asynchronous function data() {
    setTimeout(() => {
        console.log("Data is being fetched");
    }, 2000);
}




prototype inheritance : -  
                        -> Inheritance is a mechanism in which one class acquires the property of another class.
                        -> Inheritance is a mechanism in which a new class is created from an existing class.
                        -> Inheritance is a mechanism in which a class inherits the properties and behaviors of another class.
                        -> Inheritance is a mechanism in which a class acquires the properties and behaviors of another class.
                        -> Inheritance is a mechanism in which a class is created from an existing class.
                        -> Inheritance is a mechanism in which a class inherits the properties of another class.
                        -> Inheritance is a mechanism in which a class acquires the properties of another class.

                        -> Inheritance is a mechanism in which one class acquires the properties and behaviors of another class.
                        -> Inheritance is a mechanism in which a new class is created from an existing class.




Array.__proto__ : -
                    -> The Array.__proto__ property is an inbuilt property in JavaScript which is used to set the prototype of the Array constructor to another object.



Array.__proto__==Array.prototype : -
                                    -> The Array.__proto__ property is an inbuilt property in JavaScript which is used to set the prototype of the Array constructor to another object.



Array.prototype : -
                    -> The Array.prototype property represents the prototype for the Array constructor and allows you to add new properties and methods to all Array objects.

Map : -
        ->The map method in JavaScript is used to create a new array populated with the results of calling a provided function on every element in the calling array.
        ->It does not modify the original array.
        ->The map() method creates a new array with the results of calling a provided function on every element in the calling array.
